import 'package:flutter/material.dart';

class AppColors {
  // 🎨 Colores principales (tema minimalista)
  static const Color primary = Color(0xFF1ABC9C);          // Turquesa vibrante
  static const Color accent = Color(0xFF144D53);           // Azul marino oscuro para contraste

  // ☀️ Tema Claro
  static const Color lightBackground = Colors.white;       // Fondo claro principal
  static const Color lightText = Color(0xFF222222);         // Texto oscuro para mejor legibilidad
  static const Color lightCard = Color(0xFFF5F7F7);         // Fondo claro suave para tarjetas

  // 🌙 Tema Oscuro
  static const Color darkBackground = Color(0xFF121E1E);    // Fondo oscuro, con tono azul-verdoso
  static const Color darkText = Color(0xFFE0F2F1);          // Texto claro suave
  static const Color darkCard = Color(0xFF1C2B2B);          // Fondo oscuro para tarjetas, suave

  // 🧭 Extras
  static const Color borderLight = Color(0xFFCBD4D3);       // Bordes claros suaves
  static const Color borderDark = Color(0xFF2A3B3B);        // Bordes oscuros
}